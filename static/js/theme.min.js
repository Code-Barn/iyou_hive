document. addEventListener("DOMContentLoaded", function(){const toggle=document. getElementById("theme-toggle"); const html=document. documentElement; const saved=localStorage. getItem("theme")||"dark"; html. setAttribute("data-theme", saved); updateIcon(saved); if(toggle){toggle. addEventListener("click", function(){const current=html. getAttribute("data-theme"); const next=current==="dark"?"light": "dark"; html. setAttribute("data-theme", next); localStorage. setItem("theme", next); updateIcon(next);});}function updateIcon(theme){const icon=toggle&&toggle. querySelector(". theme-icon"); if(icon){icon. textContent=theme==="dark"?"☀️": "🌙";}}}); let eventPopup=null; let currentEventId=null; let isAnalyzingWithAI=false; function initPopup(){eventPopup=document. getElementById("eventPopup"); if(eventPopup){const overlay=eventPopup. querySelector(". popup-overlay"); if(overlay){overlay. addEventListener("click", closeEventPopup);}document. addEventListener("keydown", function(e){if(e. key==="Escape"&&eventPopup. classList. contains("active")){closeEventPopup();}});}}function openEventPopup(eventId){eventPopup=document. getElementById("eventPopup"); if(!eventPopup||!timelineEvents||!timelineEvents[eventId]){console. error("Popup element or event data not found"); return;}if(!eventPopup. onclick){initPopup();}currentEventId=eventId; const event=timelineEvents[eventId]; setPopupContent(eventId, event); resetAISection(); eventPopup. classList. add("active"); document. body. style. overflow="hidden"; const closeBtn=eventPopup. querySelector(". popup-close"); if(closeBtn){closeBtn. focus();}console. log(`Opened popup for event ${eventId}: ${event. title}`);}function setPopupContent(eventId, event){if(!event) return; if(document. getElementById("popupTitle")){document. getElementById("popupTitle"). textContent=event. title;}if(document. getElementById("popupDate")){document. getElementById("popupDate"). textContent=event. date;}if(document. getElementById("popupCategory")){document. getElementById("popupCategory"). textContent=event. category||"Uncategorized";}if(document. getElementById("popupDescription")){document. getElementById("popupDescription"). textContent=event. description;}renderDocuments(event);}function renderDocuments(event){const docsContainer=document. getElementById("popupDocs"); const mediaSection=document. getElementById("popupMedia"); if(!docsContainer) return; docsContainer. innerHTML=""; const docUrls=event. document_urls||[]; const supportingDocs=event. supporting_docs; if(!docUrls||docUrls. length===0){if(mediaSection){mediaSection. style. display="none";}return;}if(mediaSection){mediaSection. style. display="block";}docUrls. forEach((doc, index)=>{const docElement=document. createElement("div"); docElement. className="document-item"; const docLink=document. createElement("a"); docLink. href=doc. url||"#"; docLink. target="_blank"; docLink. rel="noopener noreferrer"; let icon="📄"; if(doc. url){const ext=doc. url. split(". "). pop(). toLowerCase(); if(ext==="pdf") icon="📕"; else if(["png", "jpg", "jpeg", "gif", "webp", "svg"]. includes(ext)) icon="🖼️"; else if(["doc", "docx"]. includes(ext)) icon="📝"; else if(["xls", "xlsx"]. includes(ext)) icon="📊"; else if(["txt", "md", "markdown"]. includes(ext)) icon="📄"; else if(["email", "eml"]. includes(ext)) icon="✉️";}docLink. innerHTML=`<span class="doc-icon">${icon}</span><span class="doc-title">${doc. title||"Document "+(index+1)}</span>`; if(doc. size||doc. type){const metaElement=document. createElement("span"); metaElement. className="doc-meta"; metaElement. textContent=`[${doc. type||"File"}${doc. size?"-"+doc. size: ""}]`; docLink. appendChild(metaElement);}docElement. appendChild(docLink); if(doc. url&&isImageUrl(doc. url)){const previewElement=document. createElement("div"); previewElement. className="doc-preview"; previewElement. innerHTML=`<img src="${doc. url}" alt="${doc. title||"Preview"}" loading="lazy">`; docElement. appendChild(previewElement);}docsContainer. appendChild(docElement);});}function isImageUrl(url){const ext=url. split(". "). pop(). toLowerCase(); return ["png", "jpg", "jpeg", "gif", "webp", "svg", "bmp"]. includes(ext);}function resetAISection(){const aiSection=document. getElementById("aiSection"); const aiResponse=document. getElementById("aiResponse"); const aiLoading=document. getElementById("aiLoading"); const askAIBtn=document. getElementById("askAIBtn"); if(aiSection) aiSection. style. display="none"; if(aiResponse) aiResponse. innerHTML=""; if(aiLoading) aiLoading. style. display="block"; if(askAIBtn){askAIBtn. disabled=false; askAIBtn. innerHTML='<span class="ai-icon">🤖</span>Ask AI About This';}isAnalyzingWithAI=false;}function askAIAboutEvent(){if(!currentEventId||isAnalyzingWithAI) return; const askAIBtn=document. getElementById("askAIBtn"); const aiSection=document. getElementById("aiSection"); const aiResponse=document. getElementById("aiResponse"); const aiLoading=document. getElementById("aiLoading"); if(!aiSection||!aiResponse||!askAIBtn) return; aiSection. style. display="block"; aiResponse. innerHTML=""; aiLoading. style. display="block"; askAIBtn. disabled=true; askAIBtn. innerHTML='<span class="ai-icon">🤖</span>Analyzing. . . '; isAnalyzingWithAI=true; fetch(`/ai/analyze-event/${currentEventId}/`). then((response)=>response. json()). then((data)=>{if(data. status==="success"){const formattedResponse=formatMarkdown(data. analysis); aiResponse. innerHTML=formattedResponse; aiLoading. style. display="none"; askAIBtn. disabled=false; askAIBtn. innerHTML='<span class="ai-icon">🤖</span>Ask AI Again';}else{aiResponse. innerHTML='<p class="ai-error">Error: '+(data. error||"Failed to get AI analysis")+"</p>"; aiLoading. style. display="none"; askAIBtn. disabled=false; askAIBtn. innerHTML='<span class="ai-icon">🤖</span>Try Again';}}). catch((error)=>{console. error("AI analysis error: ", error); aiResponse. innerHTML='<p class="ai-error">Error: Failed to connect to AI service. Please check your configuration. </p>'; aiLoading. style. display="none"; askAIBtn. disabled=false; askAIBtn. innerHTML='<span class="ai-icon">🤖</span>Try Again';}). finally(()=>{isAnalyzingWithAI=false;});}function formatMarkdown(markdown){if(!markdown) return "<p>No response from AI. </p>"; let html=markdown. replace(/&/g, "&amp; "). replace(/</g, "&lt; "). replace(/>/g, "&gt; "); html=html. replace(/^#\s+(. *?)$/gm, "<h2>$1</h2>"); html=html. replace(/^##\s+(. *?)$/gm, "<h3>$1</h3>"); html=html. replace(/^###\s+(. *?)$/gm, "<h4>$1</h4>"); html=html. replace(/\*\*(. *?)\*\*/g, "<strong>$1</strong>"); html=html. replace(/\*(. *?)\*/g, "<em>$1</em>"); html=html. replace(/^\-\s+(. *?)$/gm, "<li>$1</li>"); html=html. replace(/^\d+\. \s+(. *?)$/gm, "<li>$1</li>"); html=html. replace(/((?: <li>. *<\/li>\n)+)/g, function(match){return "<ul>"+match. trim(). replace(/<\/li>\n/g, "</li>")+"</ul>";}); html=html. replace(/\n\n+/g, "</p><p>"); html=html. replace(/\n/g, "<br>"); html="<p>"+html+"</p>"; html=html. replace(/`(. *?)`/g, "<code>$1</code>"); html=html. replace( /\[(. *?)\]\((. *?)\)/g, '<a href="$2" target="_blank" rel="noopener">$1</a>',); html=html. replace(/^---$/gm, "<hr>"); html=html. replace(/^>\s+(. *?)$/gm, "<blockquote>$1</blockquote>"); return html;}function closeEventPopup(){if(!eventPopup){eventPopup=document. getElementById("eventPopup");}if(eventPopup){eventPopup. classList. remove("active"); document. body. style. overflow=""; resetAISection(); currentEventId=null; const activeEvent=document. querySelector(". timeline-event: focus"); if(activeEvent){activeEvent. blur();}}}function closePopup(eventId){closeEventPopup();}function showPopup(eventId){openEventPopup(eventId);}document. addEventListener("DOMContentLoaded", function(){initPopup(); document. querySelectorAll('[id^="popup-"]'). forEach((popup)=>{const closeBtn=popup. querySelector(". popup-close"); if(closeBtn){closeBtn. addEventListener("click",()=>{popup. classList. remove("active");});}});}); eventPopup=document. getElementById("eventPopup"); document. addEventListener("click", function(e){if(eventPopup&&eventPopup. classList. contains("active")){if(!eventPopup. contains(e. target)){closeEventPopup();}}}); 