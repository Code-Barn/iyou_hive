📁 **Project Structure: `hiver_django`**

---

### **Backend (Django)**
```
hiver_django/
├── config/
│   ├── settings.py          # Django settings (DEBUG, DATABASE, CORS, etc.)
│   ├── urls.py              # Root URL config (Task 2 ✅)
│   └── wsgi.py
│
├── apps/
│   ├── core/
│   │   ├── models.py          # Case model
│   │   ├── views.py          # react_app_view (Task 3 ✅), api_case_list ✅
│   │   ├── urls.py           # /core/api/cases/ ✅
│   │   ├── templatetags/
│   │   │   └── assets.py     # get_vite_assets tag (Task 4 ✅)
│   │   └── middleware.py      # Auth middleware (Task 1 ✅)
│   │
│   ├── timeline/
│   │   ├── models.py          # TimelineEvent + section_header, last_printed_citation
│   │   ├── api_views.py      # generate_pdf(), citation manifest
│   │   ├── views.py           # legacy views
│   │   ├── services/
│   │   │   └── legacy_services.py  # create_timeline_event + smart attribution ✅
│   │   └── urls.py
│   │
│   ├── archive/
│   │   └── models.py, views.py, api_urls.py
│   │
│   ├── ai_assistant/
│   │   ├── services.py      # NEW: AIService.classify_party() (Task 4)
│   │   └── api_client.py
│   │
│   └── accounts/
│       └── urls.py, views.py
│
├── scripts/
│   └── timeline_to_pdf.py    # PDF generation + citation manifest
│
├── templates/
│   └── frontend/
│       └── index.html        # Uses get_vite_assets tag ✅
│
├── static/
│   └── frontend/
│       ├── index.html        # Built by Vite (updated via tag)
│       ├── assets/
│       │   ├── index-XXX.js
│       │   ├── index-XXX.css
│       │   └── manifest.json   # Generated by Vite
│       └── .vite/
│           └── manifest.json   # Actual manifest location ✅
│
├── manage.py
└── .venv/                    # Python virtual environment
```

---

### **Frontend (React + Vite)**
```
frontend/
├── src/
│   ├── App.tsx                  # Uses Layout component ✅
│   ├── main.tsx                 # Entry point (DOMContentLoaded wrapper ✅)
│   ├── vite.config.ts           # base: '/static/frontend/', manifest: true ✅
│   │
│   ├── components/
│   │   ├── Layout.tsx             # NEW: 3-pane dashboard + full-screen toggle ✅
│   │   ├── CaseSelector.tsx        # Case picker + create form ✅
│   │   ├── TimelineToolbar.tsx    # Modal popups (upload, add event) ✅
│   │   ├── FileTree.tsx           # Archive pane (fixed syntax errors ✅)
│   │   ├── DocumentPreviewModal.tsx # NEW: Full-screen doc preview ✅
│   │   ├── DiffView.tsx
│   │   ├── SimplifiedView.tsx
│   │   ├── EventCard.tsx          # Shows citation badge ✅
│   │   ├── InspectorPanel.tsx
│   │   └── ...
│   │
│   ├── api/
│   │   ├── timeline.ts
│   │   └── archive.ts
│   │
│   └── types/
│       └── timeline.ts            # TimelineEvent + last_printed_citation ✅
│
├── index.html                  # Source HTML (for Vite build)
├── package.json
└── node_modules/                # npm dependencies
